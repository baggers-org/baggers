import { marketDataAdapter } from './market-data-adapter';

describe('marketDataAdapter', () => {
  it('should work', () => {
    expect(marketDataAdapter()).toEqual('market-data-adapter');
  });
});
