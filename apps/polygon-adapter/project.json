{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/polygon-adapter/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/apps/polygon-adapter",
        "main": "apps/polygon-adapter/src/main.ts",
        "tsConfig": "apps/polygon-adapter/tsconfig.app.json",
        "generatePackageJson": true
      },
      "configurations": {
        "watch": {
          "watch": true
        },
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        }
      }
    },
    "setup": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx arc init"
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "polygon-adapter:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "polygon-adapter:build:production"
        }
      }
    },
    "dev": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "polygon-adapter:build:watch"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "apps/polygon-adapter/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": [
        "coverage/apps/polygon-adapter"
      ],
      "options": {
        "jestConfig": "apps/polygon-adapter/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "flyctl deploy -c apps/polygon-adapter/fly.staging.toml"
      },
      "configurations": {
        "production": {
          "command": "flyctl deploy -c apps/polygon-adapter/fly.toml"
        }
      }
    }
  },
  "tags": []
}