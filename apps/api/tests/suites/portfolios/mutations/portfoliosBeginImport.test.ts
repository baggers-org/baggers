import { User1Sdk } from "~test-sdk";

export const portfoliosBeginImportTests = () =>
  describe('portfoliosBeginImport', () => {
    it('should create a PortfolioImportDraft for a users plaid transactions imported from a broker', () => {
	User1Sdk().portfoliosBeginImport({
		publicToken: 
	})



    });
  });
