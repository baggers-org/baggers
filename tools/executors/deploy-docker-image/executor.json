{
  "executors": {
    "deploy-docker-image": {
      "implementation": "./deploy-docker-image.js",
      "schema": "./schema.json",
      "description": "Docker build and push on affected packages"
    }
  }
}